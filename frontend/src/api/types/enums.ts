export enum UserRole {
  admin = "admin",
  researcher = "researcher",
  field_operator = "field_operator",
}

export enum StationStatus {
  active = "active",
  inactive = "inactive",
  maintenance = "maintenance",
  offline = "offline",
}

export enum DeviceStatus {
  online = "online",
  offline = "offline",
  unassigned = "unassigned",
}

export enum StationUserRole {
  owner = "owner",
  researcher = "researcher",
  field_operator = "field_operator",
}
